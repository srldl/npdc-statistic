// var arcgisToGeoJSON = require('./dist/arcgis-to-geojson.js').arcgisToGeoJSON;
// var geojsonToArcGIS = require('./dist/arcgis-to-geojson.js').geojsonToArcGIS;

var foo = require('./dist/arcgis-to-geojson.js');

var geojson = foo.arcgisToGeoJSON({
  'attributes': {
    'PROJ_DESC': 'REMODEL FOR DRINK STATION ON FLOOR 3. C/C                         '
  },
  'geometry': {
    'x': null,
    'y': null
  }
});
console.log(geojson);

// parse ArcGIS JSON, convert it to GeoJSON
/*
var geojson = arcgisToGeoJSON({
    "x":-122.6764,
    "y":45.5165,
    "spatialReference": {
      "wkid": 4326
    }
  });

console.log(geojson);

// take GeoJSON and convert it to ArcGIS JSON
var arcgis = geojsonToArcGIS({
  "type": "Point",
  "coordinates": [45.5165, -122.6764]
});

console.log(arcgis);
*/
